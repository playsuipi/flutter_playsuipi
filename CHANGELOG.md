## 1.2.0

* Bump core library to 1.2.0:
   - Add point cards are not dealt to the floor rule.

## 1.1.0

* Bump core library to 1.1.0:
    - Fix 2 + 3 + 2 is a valid build rule.
    - Add cannot orphan owned pile rule.

## 1.0.2

* Fix implicit assumption of Rust `nightly` toolchain availability. This allows
  iOS to build without needing to manually set the Rust toolchain to `nightly`.

## 1.0.1

* Fix iOS and macOS build issues.

## 1.0.0

* Add the Play Suipi Core library to Flutter using FFI.
